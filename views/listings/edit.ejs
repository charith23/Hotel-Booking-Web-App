<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Listing | WanderLust</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet" />
  </head>
  <body>
    <header>
      <h1>WanderLust</h1>
      <nav>
        <a href="/listings">Home</a>
        <a href="/listings/new">Add Listing</a>
      </nav>
    </header>
    <div class="container">
      <h2 style="color:#ff385c; font-family:'Montserrat',Arial,sans-serif;">Edit your Listing</h2>
      <form method="POST" action="/listings/<%= listing._id %>?_method=PUT" style="display:grid; grid-template-columns: 1fr 1fr; gap:1.5rem; align-items:start;">
        <div style="display:flex; flex-direction:column; gap:1rem;">
          <label style="font-weight:600;">Title</label>
          <input name="listing[title]" value="<%= listing.title %>" type="text" required />
          <label style="font-weight:600;">Image URL</label>
          <input name="listing[image]" value="<%= listing.image %>" type="text" />
          <label style="font-weight:600;">Price</label>
          <input name="listing[price]" value="<%= listing.price %>" type="number" required />
        </div>
        <div style="display:flex; flex-direction:column; gap:1rem;">
          <label style="font-weight:600;">Description</label>
          <textarea name="listing[description]" required style="min-height:120px;"><%= listing.description %></textarea>
          <label style="font-weight:600;">Country</label>
          <input name="listing[country]" value="<%= listing.country %>" type="text" required />
          <label style="font-weight:600;">Location</label>
          <input name="listing[location]" value="<%= listing.location %>" type="text" required />
        </div>
        <div style="grid-column:1/3; margin-top:1rem;">
          <button class="btn" style="width:100%;">Edit</button>
        </div>
      </form>
    </div>
    <%- include('../partials/footer') %>
  </body>
</html>
